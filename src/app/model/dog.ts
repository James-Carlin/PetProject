export class Dog {
  id: number;
  breed: string;
  imageURL: string;

  constructor(id: number, breed: string, imageURL: string){
    this.id = id;
    this.breed = breed;
    this.imageURL = imageURL;
  }
}
